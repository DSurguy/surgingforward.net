import type { Metadata } from 'next'
import LoadableContent from '@/sharedComponents/LoadableContent';

export const metadata: Metadata = {
  title: 'Derek Surguy | Contact',
}

export default function ContactPage() {
  return <>
    <LoadableContent>
      <div>Test</div>
    </LoadableContent>
  </>
}